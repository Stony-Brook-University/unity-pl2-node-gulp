<div data-match-height="polaroid-card" href="#" class="polaroid-card">
<img class="polaroid-card--image" src="{{img.landscape-16x9.src}}" />
	<h2 class="polaroid-card--heading">{{headline.short}}</h2>
	<div class="polaroid-card--text">{{excerpt.short}}</div>


	{{# cta }}
    <div class="polaroid-card--cta">
        <a href="{{cta.url}}" class="{{cta.class}}">{{cta.text}}</a>
    </div>
    {{/ cta }}

	</div>
